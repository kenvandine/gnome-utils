
INCLUDES = 			\
	-I$(includedir)

bin_PROGRAMS = gpenguin

gpenguin_SOURCES = \
	gpenguin.c

gpenguin_LDADD = $(GTK_LIBS)

pixmapdir = $(datadir)/pixmaps

BITMAPS = penguin1.xpm

pixmap_DATA = $(BITMAPS)

Utilitiesdir = $(datadir)/apps/Utilities

EXTRA_DIST = penguin1.xpm
