
INCLUDES = 			\
	-I$(includedir)

bin_PROGRAMS = grun

grun_SOURCES = \
	grun.c

grun_LDADD = $(GTK_LIBS)

Utilitiesdir = $(datadir)/apps/Utilities
Utilities_DATA = grun.desktop

EXTRA_DIST = grun.desktop
