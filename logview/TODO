
- Use some kind of authentification tool (libgnomesu ? gksudo ?) to ask for the root password at launch.

- Use gnome-vfs (if safe) to monitor the logs instead of a timeout.

- Simplify the zoom dialog (the one that comes up with double-click) - there's
  really no need for a gtk_tree_list and store and all that stuff just to show
  4 fields.